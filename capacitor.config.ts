import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'RegistrAPP_P1_Alvarez_Navarro',
  webDir: 'www'
};

export default config;
